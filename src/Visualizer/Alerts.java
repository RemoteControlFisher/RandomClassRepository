package Visualizer;

/**
 * Manages user inputs on "What is alarming weather data," and fires off alerts when data exceeding the users parameters on alarming weather happens.
 */
public interface Alerts {
	
	
	/**
	 * A method that can be used to open the alerts window, so the user can access the fields and inputs there.
	 */
	void openRequestWindow();
}
